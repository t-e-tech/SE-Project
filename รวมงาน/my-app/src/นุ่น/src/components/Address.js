import { Component } from "react";
import React from "react";

export default class Address extends Component{
    render(){
        return(
            <h2>
                choose a Address
            </h2>
        );
    }
}